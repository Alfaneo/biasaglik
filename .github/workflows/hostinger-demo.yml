# Her push'ta siteyi derler ve Hostinger'daki demo subdomain'e FTP ile yükler.
# Gerekli GitHub Secrets: FTP_SERVER, FTP_USERNAME, FTP_PASSWORD
# İsteğe bağlı GitHub Variable: FTP_DIR (varsayılan ./ — FTP hesabı zaten subdomain klasörüne açılıyorsa)
name: Hostinger demo yükleme

on:
  push:
    branches: [main, claude/eloquent-mendel-8fiqyt]
  workflow_dispatch: # Actions sekmesinden elle çalıştırma düğmesi

concurrency:
  group: hostinger-demo
  cancel-in-progress: false # yükleme yarıda kesilmesin

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Kodu indir
        uses: actions/checkout@v4

      - name: Node.js kur
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Paketleri kur
        run: npm ci

      - name: Siteyi derle (demo modu, Google'dan gizli)
        run: npm run build
        env:
          DEMO: 'true'

      - name: FTP bilgileri var mı?
        id: kontrol
        env:
          FTP_SERVER: ${{ secrets.FTP_SERVER }}
          FTP_USERNAME: ${{ secrets.FTP_USERNAME }}
          FTP_PASSWORD: ${{ secrets.FTP_PASSWORD }}
        run: |
          eksik=""
          [ -z "$FTP_SERVER" ] && eksik="$eksik FTP_SERVER"
          [ -z "$FTP_USERNAME" ] && eksik="$eksik FTP_USERNAME"
          [ -z "$FTP_PASSWORD" ] && eksik="$eksik FTP_PASSWORD"
          if [ "$eksik" = " FTP_SERVER FTP_USERNAME FTP_PASSWORD" ]; then
            echo "::warning::FTP secret'ları tanımlı değil, yükleme atlandı. Settings → Secrets and variables → Actions bölümünden ekleyin."
            echo "atla=true" >> "$GITHUB_OUTPUT"
          elif [ -n "$eksik" ]; then
            echo "::error::Eksik secret:$eksik — Settings → Secrets and variables → Actions bölümünde adları birebir kontrol edin."
            exit 1
          fi

      - name: Hostinger'a yükle
        if: steps.kontrol.outputs.atla != 'true'
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: ./dist/
          server-dir: ${{ vars.FTP_DIR || './' }}
          log-level: standard
